{
  "name": "do-not-click-this-button",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "start": "vite --host 127.0.0.1",
    "check": "node scripts/check.js && node scripts/gameplay-check.js && node scripts/arena-check.js",
    "smoke:browser": "node scripts/browser-smoke.js",
    "smoke:arena": "node scripts/arena-smoke.js"
  },
  "dependencies": {
    "phaser": "3.90.0",
    "vite": "latest"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0"
  }
}
